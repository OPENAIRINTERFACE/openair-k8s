apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oai-spgwc.fullname" . }}-test-connection"
  labels:
    {{- include "oai-spgwc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oai-spgwc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
