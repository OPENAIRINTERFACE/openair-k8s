apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oai-mme.fullname" . }}-test-connection"
  labels:
    {{- include "oai-mme.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: ping
      image: busybox
      command: ['ping']
      args: ['-c' , '1', {{ include "oai-mme.fullname" . }} ]
  restartPolicy: Never
